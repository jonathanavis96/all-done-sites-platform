import { defineConfig } from "vite"
import react from "@vitejs/plugin-react"

export default defineConfig({
  plugins: [react()],
  // Important for GitHub Pages so assets resolve correctly
  base: "/all-done-sites-platform/",
})
